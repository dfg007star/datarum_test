$('.form-control-corrections').val('');
$('#collapseCor<%= @correction.answer_id %>').collapse('hide');
$(`#correction<%= @correction.answer_id %>`).html('<%= j render :partial => 'corrections/corrections',
  :collection =>  @corrections %>')