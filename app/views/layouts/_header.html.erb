<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="#">Datarum Test</a>
      <ul class="nav navbar-nav" >
        <% if user_signed_in? %>
          <div class="row align-items-center">
            <p class="text-white mr-3 mb-0"><%= current_user.username %></p>
            <li><%= button_to 'Sign out',  destroy_user_session_path, :method => :delete, class: "btn btn-sm btn-danger" %></li>
          </div>
        <% else %>
          <div class="row">
            <li class="mr-2"><%= link_to 'Sign in', new_user_session_path, class: "btn btn-sm btn-primary" %></li>
            <li><%= link_to 'Sign up', new_user_registration_path, class: "btn btn-sm btn-secondary"%></li>
          </div>
        <% end %>
      </ul>
  </div>
</nav>
